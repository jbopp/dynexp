# This file is part of DynExp.

target_sources(DynExpHardware PRIVATE "HardwareAdapterEthernet.cpp" "HardwareAdapterEthernet.h")
target_sources(DynExpHardware PRIVATE "HardwareAdapterSerialPort.cpp" "HardwareAdapterSerialPort.h")
target_sources(DynExpHardware PRIVATE "HardwareAdaptergRPC.cpp" "HardwareAdaptergRPC.h")

if (USE_NIDAQ)
	target_sources(DynExpHardware PRIVATE "HardwareAdapterNIDAQ.cpp" "HardwareAdapterNIDAQ.h")
	list(APPEND EXTRA_LIBS "NIDAQmx")
endif()
if (USE_PVCAM)
	target_sources(DynExpHardware PRIVATE "HardwareAdapterPVCam.cpp" "HardwareAdapterPVCam.h")
	list(APPEND EXTRA_LIBS "pvcam64")
endif()
if (USE_QUTOOLSQUTAG)
	target_sources(DynExpHardware PRIVATE "HardwareAdapterQutoolsTDC.cpp" "HardwareAdapterQutoolsTDC.h")
	list(APPEND EXTRA_LIBS "tdcbase")
	list(APPEND SHARED_LIBS "tdcbase")
	list(APPEND SHARED_LIBS "libgcc_s_seh-1")
	list(APPEND SHARED_LIBS "libstdc++-6")
	list(APPEND SHARED_LIBS "libusb0")
	list(APPEND SHARED_LIBS "libwinpthread-1")
	if (QUTOOLSQUTAG_VARIANT STREQUAL "S")
		list(APPEND SHARED_LIBS "FTD3XX")
	endif()
endif()
if (USE_SMARACT)
	target_sources(DynExpHardware PRIVATE "HardwareAdapterSmarAct.cpp" "HardwareAdapterSmarAct.h")
	list(APPEND EXTRA_LIBS "SmarActCTL")
	list(APPEND SHARED_LIBS "SmarActCTL")
	list(APPEND SHARED_LIBS "SmarActIO")
endif()
if (USE_ZIMFLI)
	target_sources(DynExpHardware PRIVATE "HardwareAdapterZILabOne.cpp" "HardwareAdapterZILabOne.h")
	list(APPEND EXTRA_LIBS "ziAPI-win64")
	list(APPEND SHARED_LIBS "ziAPI-win64")
endif()
if (USE_SWABIANPULSESTREAMER)
	list(APPEND PROTO_FILES "./include/SwabianInstruments/pulse_streamer.proto")
	target_sources(DynExpHardware PRIVATE "HardwareAdapterSwabianInstrumentsPulseStreamer.cpp" "HardwareAdapterSwabianInstrumentsPulseStreamer.h")
endif()

set(EXTRA_LIBS ${EXTRA_LIBS} PARENT_SCOPE)
set(SHARED_LIBS ${SHARED_LIBS} PARENT_SCOPE)
set(PROTO_FILES ${PROTO_FILES} PARENT_SCOPE)